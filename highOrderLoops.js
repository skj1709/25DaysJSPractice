//for of

const arr = [1, 2, 3, 4, 5];

for (const ele of arr) {
  console.log(ele);
}

const greeting = "hello world";
const charArr = [];
for (let char of greeting) {
  charArr.push(char);
}
console.log(charArr);

// Maps

const map = new Map();
